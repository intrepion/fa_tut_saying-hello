# Finish

Start the Flutter mobile app from the repository root:

```bash
just run
```

On macOS, `just run` defaults to the iOS simulator. To target Android instead, use:

```bash
just run device=android
```

Try these inputs:

- enter `Ada` and expect `Hello, Ada!`
- submit an empty value and expect `Hello!`
